Triangle is used as meshing util:

$ wget -r -nH --cut-dirs=2 --no-parent --reject="index.html*" https://people.sc.fsu.edu/~jburkardt/c_src/triangle/

-> build script will compile triangle as library.

TODO:
    - out of tree build with CMake
    - benchmark optimization
    - command line arguments (sparse implementation, ...)
    - add build/ to .gitignore after CMake is done
    - valgrind tests
